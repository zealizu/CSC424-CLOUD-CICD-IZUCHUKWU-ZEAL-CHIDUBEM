// DOM 
/**the Document Object Model (DOM) is an API that represents the structure of an HTML or XML document as a tree-like structure. It provides methods and properties to interact with and 
 * manipulate the elements, attributes, and text content of a web page.

The DOM represents an HTML document as 
a hierarchy of nodes, with the document node
 at the top. Each node corresponds to an element, 
 attribute, or text within the document. You can access and modify 
 these nodes using JavaScrip */
 